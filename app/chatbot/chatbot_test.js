'use strict';

describe('TN_App.chatbot module', function() {

  beforeEach(module('TN_App.chatbot'));

  describe('chatbot controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var chatbotCtrl = $controller('chatbotCtrl');
      expect(chatbotCtrl).toBeDefined();
    }));

  });
});